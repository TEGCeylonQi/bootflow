<!--
段落格式约定（tools/release-notes.mjs 依赖，改这个文件前先看这一段）：

- 每个版本一节，标题用二级标题 `## v0.2.1`（tag 名去掉 `v` 前缀即标题里的版本号）
- 节与节之间用 `---` 分隔。**最后一节后面不要再留 `---`**——
  提取逻辑取到文件末尾，多出来的分隔线会原样拼进发布页正文
- 正文用 `-` 无序列表，写「对外可见的变化」，不写实现细节
- 只有一条变更也要有一行，别写「本次无修复」这类空话
- 本注释块写在第一个 `##` 之前，所以永远不会被提取进正文
-->

# BootFlow 发布记录

发布页的正文由本文件生成：`tools/release-notes.mjs` 按 tag 取出对应段落，
拼上通用说明后写入 Release。所以每个版本只写**对外可见的变化**。

## v0.2.1

> 本版取代 v0.2.0 —— 该版本的安装包与便携版启动即退出，已撤回。

- **耗时分析页重做**：四张图各配一行口径，长解释收进折叠的「数据口径与来源」。
  单项数据由列表改为图表——开机相位、每项的出现时刻、每项的资源占用、
  最近几次开机的总时长，四条通路在同一屏内各就各位，不用在几个列表之间对数字。
- 修正「启动影响」图的口径：条长改为 **CPU 时间**（有真实刻度），
  不再把 CPU 与磁盘折算成一个无量纲分数；档位色仍是 Windows 的判定，磁盘量在悬浮里给。
- 各阶段图与单项时间轴的标签按底色明暗自动取前景色：深蓝相位段上的文字此前读不出来。
- 其余能力不变：全来源扫描、风险分级与处置建议、可逆的启用 / 停用
  （预演 → 快照 → 回滚）、报告导出（JSON / CSV / Markdown）。
