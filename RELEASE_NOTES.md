# BootFlow 发布记录

每个版本的更新日志都以「发布页正文」为准，这个文件是发布页正文「本版本更新」段的来源。
发布时执行 `tools/release-notes.mjs` 会把**当前 tag 对应的段落**拼进 Release 正文——
所以每一节只写这个版本对外可见的变化，格式要求见文件末尾。

## v0.2.1

**修复 v0.2.0「一打开就退出」的问题。**

- v0.2.0 的安装包与便携版都有这个毛病：程序启动瞬间退出，**屏幕上没有任何提示**
  （不弹窗、不报错、窗口都不出现，看起来就是「双击没反应」）。已下载 v0.2.0 的请换用本版。
- **触发条件**：只要在设置里打开过「每次开机自记账」，之后每次启动都会崩。
  这个开关默认关闭，所以刚装好时能正常打开，打开过它之后才开始打不开。
  **v0.1.5 及更早版本没有这个问题。**
- **原因**：这个开关的启动自检里，有一句代码被写成了「函数最后返回的表达式」，
  导致一个计划任务对象的释放被推迟到 COM 运行时**关闭之后**——而关闭 COM 会卸载
  进程内的任务计划程序组件，此时再去释放它，就是在调用一段**已被卸载的代码**。
- **修复**：让该对象显式先于 COM 运行时释放，并把这条约束写进 `util/com.rs` 的模块文档；
  同时把项目里其余 8 处同类调用都核对了一遍，**只有这一处写错**。

其余功能与 v0.2.0 相同。

## v0.2.0

- **从「只看」变成「能改」——BootFlow 第一次可以真正停用启动项**：
  - 编排模式里把要处理的项勾进变更篮，点「应用」**先看到一份预演**：
    每一项改成什么（改动前 → 改动后）、会发生什么、风险高低，都在动手之前摆出来
  - 应用前**自动建立改前快照**；不想要了随时一键回滚到改动前，历史快照可查看、可导出
  - 写入过程失败会自动回退已改的部分，不会留下「改了一半」的状态
- **不可逆的东西仍然不给碰**：服务栈、Winlogon、explorer、安全中心、驱动这些禁改区
  连预演都不会出现；服务的启动类型只允许在「自动 ↔ 手动」之间切换，
  不提供「禁用」——那是个只能往前走、没法回头的门
- **不再请求提权——不会再有 UAC 弹窗**：exe 改为以启动它的用户权限运行
  （manifest `asInvoker`），双击即开。之前几版声明的是「以可用最高权限运行」，
  管理员账户下**每次启动都要点一次 UAC**，放进开机自启还会在登录时弹窗。
  核心功能不依赖提权：启动项全量扫描、风险评级、诊断建议，以及每次开机的总时长
  （读的是 System 通道，该通道允许普通用户读）在普通权限下都完整可用。
  只有分段耗时与「启动影响」需要管理员，读不到时界面会写明原因并给出
  「以管理员身份重开」入口，由你决定要不要提，**不带伪造精度**
- **每次开机的总时长可以自己记了（可选，默认关闭）**：
  Windows 只在开机偏慢时才写记录，开了快速启动的机器可以几个月一条都没有。
  打开这个开关后，BootFlow 每次开机静默记一条开机总时长，耗时分析页会画出
  最近几次开机的趋势。这是本项目里少数几个会往系统里放东西的操作，所以
  默认关闭、开关可随时关掉并清理干净
- **新增「启动影响」——每一项到底占了多少资源**：
  - 读的是 Windows 自己留下的启动开销记录，与任务管理器「启动应用」页的
    「启动影响」同源；按微软的阈值分成高 / 中 / 低
  - 显示的是 **CPU 时间与磁盘读写量，不是耗时**。CPU 时间跨核累加，
    一项可以超过开机窗口本身，与「启动花了多久」是两回事，界面分开呈现
  - 这一项需要管理员权限；读不到时如实说明并给出提权入口，不用空数据充数
- **新增「每一项在开机后第几秒出现」**：进程采样得到，不需要管理员权限。
  明确标注这是**出现时刻，不是耗时**——「开机后 12 秒才出现」跟「启动花了 12 秒」
  完全不是一回事，界面上不会混成一个数字
- 清单新增排序切换：**按开机顺序** / **按资源占用**。
  两个问题是两码事，不合成一个「综合分」；没有影响数据的项沉底，
  因为「不知道」不等于「占得少」
- 导出报告（JSON / CSV / Markdown）同步带上启动影响，CSV 多一列、Markdown 多一节
- 修掉一批把 `**加粗**`、反引号原样显示到界面上的文案问题

## v0.1.5

- **「每次开机都记录」引导**：读到不到、或系统还没记录开机的耗时时，界面新增
  「查看能否开启」卡片，说清 Windows 默认只在开机偏慢时记录、快速启动下「关机」
  不算完整引导——想看数据，用「完整重启」而不是「关机再开」
- **更新下载显示真实进度**：下载安装包时不再只转圈，而是显示
  「已下载 / 总大小 + 百分比」的进度条，下载完成自动拉起安装向导
- 若系统策略把开机性能诊断禁用了，一键恢复允许记录（15 分钟后自动还原原值，可逆）

## v0.1.4

- **「检查更新」可以直接下载并安装，不再只是跳去发布页**：
  - 面板里新增「下载并安装」按钮，选好安装包后点一下，程序自动下载到缓存目录、
    用系统默认方式打开安装向导
  - 安装向导唤起成功后自动清理下载缓存；安装被取消留下的残留文件，
    可在面板右下角「清理下载缓存」手动清掉
- **安装包地址只认本仓库的 Releases 下载段**：两层校验确保交给系统 shell
  的只会是 `github.com/TEGCeylonQi/bootflow/releases/download/` 下的地址，
  即便 GitHub 接口返回被改道也不会执行别的东西
- 下载沿用检查更新的同一套通道：系统代理、系统根证书，不引第三方依赖
- 发布记录与版本说明以「发布页」为唯一权威来源，软件内不再重复维护更新日志

## v0.1.3

- **权限方案修正**：管理员权限从「强制提权」改为「以可用最高权限运行」——
  管理员账号下启动弹一次 UAC 自动提权；拒绝 UAC、UAC 被关闭或非管理员账号时，
  程序仍以普通权限照常启动，只有开机时长那一层读不到（界面如实说明）

## v0.1.2

- **以管理员身份运行**：安装包声明了最高可用权限，管理员账号下打开即提权，
  不再需要「右键 → 以管理员身份运行」
- **开机日志一键引导**：读不到开机耗时时，界面直接带你打开 Windows 事件查看器
  对应通道，不用自己翻系统日志
- 界面精简：去掉重复入口，聚焦「清单 + 详情 + 批量操作」
- 导出三格式（JSON / CSV / Markdown）接入真实后端

## v0.1.1

- 顶栏新增「检查更新」，启动后自动问一次 GitHub，发现新版本会亮提示
- 检查失败如实报告原因，绝不谎报「已是最新」
- 打开发布页走域名白名单，只放行 GitHub 自家地址

## v0.1.0

- 首个只读体检版，四个来源全量扫描：
  启动文件夹、注册表 Run / RunOnce、计划任务、系统服务
- 三档评级识别危险项；检测失效与残留的启动项
- 开机耗时时间轴（实测 / 估算 / 未知三档口径）
- 导出 JSON / CSV / Markdown 报告

---

**段落格式约定**（`tools/release-notes.mjs` 依赖）：

- 每个版本一节，标题用二级标题 `## v0.1.4`（tag 名去掉 `v` 前缀即本文件标题里的版本号）
- 节与节之间用 `---` 分隔
- 正文用 `-` 无序列表，写「对外可见的变化」，不写实现细节
- 只有一条或几条变更也要有一行，别写「本次无修复」这类空话